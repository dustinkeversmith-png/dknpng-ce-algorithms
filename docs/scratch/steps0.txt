# Evaluation and nested operator context
When executing a operator we can use our map


1. Test the base value system without operations
2. Test using fast parse and test the actual language for parsing but not building the nodal system yet


Review the entire functional folder and sub scala files including the comments which describe the system best.

# Dont erase any of my comments
# Try not to change any of the syntax, function names or desired behavior goals

Finish developing the value test so that it passes the desired behavior

Finish parser.scala to be the simplistic C++ like language without type name specifciations and semicolons as specified


sbt "testOnly com.example.MyTestSuite"

1.
ValueTypeNormalize
// This parses a fields input into uniform string to ValueTypes, although I dont like actually having a BaseValueType entire new class for this IMO.

2. Why do we need a ValueTypeImpl class, I dont understand, just one ValueType class will do or one object with a constructor would be easier for me to understand

3. Equals is going to need to be an actual algorithm, like iterating through the map, matching the fieldNames and ValueType.t .... not this silly one liner && && && && thing, since I dont know if fields == that.fields is just like measuring the pointer, the names, the values or doing the preferred way.

4. This hashCode is basically ass, and doesn't really specify how the shape, fields and strings are being encoded before the hash, can you please be more explicit in the code you write please.

5. Yeah if we can just combine all that ImplShit into just one class, not this weird apply apply logic...

6. Instead of normalizing the strings, is it possible to just add like a constructor with a string on the ValueType?

7. You also just removed the test entirely from value.scala, I dont know why you did that?

8. Why are we not storing the name of itself, the shape and the fields in the actual class?

9. The layout algorithm looks terrible and doesn't actually recurse to collect the correct layout information, it needs to test if its a basetype, then it will accumulate the sizes and size information from the type_registry.

10. I've gone ahead and fixed alot of these terrible coding behaviors you were doing, make it point to not do this again.

a. No applies, no stupid duplicated impl functions, one class always and forever with a constructor and members.

b. No object apply trash, one class only forever.

c. No element counts, thats stupid

d. Code the members like they are members mutating the internal state like any other programming language please

100.0
 no random private shit, everyhting public.
 
100.0
 use actual naming if you are going to create extra function names and classes, which I dont want you to do, I want tyou to keep the code contained in the code areas I create... esepcially if I go out of my way to create them...
 
ALWWAYS USE THIS AND SET INSIDE 

NO EXTRA BASE VALUETYPE MOTHER FUCKER, PERMANENT CHECKS ALWAYS




# Value precedents.
sbt "testOnly com.example.MyTestSuite"




// Run by class name only.
sbt "testOnly *TypeTests"




scala-cli src/main/scala/engine/value/value.scala

Ensure Functional Evaluation and Typing.

sbt "testOnly ParserTests"

To TestValue we will except a input 

1. Update the newly named fst library to match the updated parser.scala, computing the finalized nodal tree

2. Notice how i moved the weird args, stack out of the FunctionalSemanticTree I just wanna have a this or init for that as well its kind of confusing.

Also I would like the idea to pass a stack to the FunctionalSemanticTree but also have the option to not and it just comes with an empty one it fills up.

3. Test a full conversion of a FunctionalTree into the semantic tree to

4. Evaluate with the evaluator based on a made up mutable args, also instead of MemoryRef, can you just pass in the Value itself, and well use a value indexer to mutate the argument values underneath memory.


fst_test.scala

 Your implementation wasn't actually testing the mutability of the underlying memory so I instated ended up passing in a mutableParticle, setting its initial values, and then mutating them in the program, then make make sure their mutation make sense in fst_scala

1. Evaluator
I dont really like evaluator.write, so dont use a function like that, instead the FunctionalSemanticTree will store the args, with their Values as references for mutation, 

Then when the evaluator goes to evaluate it will read the args from the specified SemanticTree or FST

FST
I went ahead and just got rid of the base construtor, I dont like those if you could not use those, I prefer to store all memebrs in the class and have individual constructs setters etc

Evaluator

in VariableNodes now we dont have to resolve the node, we have the args which have already resolved and mapped them selve

we are going to also not lump member access and index all in one ya see

ive written psuedo code of whats supposed to happen here,

I've removed the resolve feature, that was dumb, we dont need that, just use smart caching of the hierarchical things you need.

I got rid of read and write, dont bring those back.

Youll read and write just using functions defined on the Value for now until we get operators ready.

Also we dont need to convert any values to any numbers or anything like that, we always just use Value

so in the + case for example we assume the value will have a operator 

I also got rid of the weird | lines in front of the statements, I dont want that.

also using the assertEquals functions for now is kind of out of style, it might be best to use our own evaluation which will be the next part I will go over.

youll see the expected syntax changes based on my changes to the fst_test

and also Ill be changing the stac


So now how this is going to work is that we have BaseTypes 

BaseTypes
 add operator overloads for all base c-style types, start with the TypeRegistry empty and then just add extension builders
 
This will then help aid in finishing the value layout system to be intelligent and understand what baseTypes are




sbt "testOnly TypeTests"

sbt "testOnly FunctionalSemanticTreeIntegrationTests"
sbt "testOnly FunctionalAstTests"

most likely well start with the basetypes registry, register all general base types and operators, which will require the ast and semantic evaluation.

What do I need to do? I need to make sure my syntax trees look good, and hook up my basetypes operator registry to actually test automatic series of value descriptions, one test for base line, one test for recursion

Then baseline operators, and operators recursion

Alright this going pretty good just got to change a view things around here.

instead of getting a operators for the actual type instead, you are going to use typenames and only one operator registry in the type registry.


this is called operators in the TypeRegistry if you noticed.

so we are no longer goign to be returning ValueOperators from register type, we are also 

we also no longer need strange functions in the registry such as literal or value, that is bad.

I also removed TypeRegistry, object instead we just create a new TypeRegistry object and pipelin all the stuff, no need to create a new object type for this just one function populating a new TypeRegistry object.

I transitioned BaseTypes object, into base helper, now the syntax of registry needs to be fixed to fit the actual expected paradigm in the comments of operators.scala

using our handy dandy FunctionalId

no more will we use this left.comparison etc that is garbage and stupid. no more.

# Spaces
# Perfect Problem Definition