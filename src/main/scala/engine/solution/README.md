// Should mirror the context operations and strategy section but here the operations are as small as single line instruction types and control structures, one operation can be like a look and also embedd children operations like a thign
// So in total there are multiple axes of optimization/building

// We need an assembler which can alter topology of operations, strategies for both
// Then the choice or decision mechanism can choose to use the assembler to create steps etc for both
// So the operations will have steps etc.